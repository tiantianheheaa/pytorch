## PyTorch 中常见的数据变换操作总览

PyTorch 中“数据变换”通常指 **不改变或改变 Tensor 形状、维度顺序、存储布局、拼接拆分方式、广播方式** 等操作。常见操作可以按用途分为以下几类：

| 类别 | 常用操作 |
|---|---|
| **形状变换** | `view`、`reshape`、`flatten`、`squeeze`、`unsqueeze` |
| **维度交换 / 重排** | `transpose`、`permute`、`movedim` |
| **连续性处理** | `contiguous` |
| **扩展 / 复制** | `expand`、`repeat`、`tile` |
| **拼接 / 堆叠** | `cat`、`stack` |
| **拆分** | `split`、`chunk`、`unbind` |
| **索引 / 选择** | `index_select`、`gather`、`take`、布尔索引 |
| **维度广播** | broadcasting、`broadcast_to` |
| **类型 / 设备转换** | `to`、`float`、`long`、`cuda`、`cpu` |

下面按实际使用频率展开说明。

---

# 1. `view`：改变 Tensor 形状

## 作用

`view()` 用于在 **不改变数据内容** 的情况下改变 Tensor 的形状。

```python
import torch

x = torch.randn(2, 3, 4)
y = x.view(6, 4)

print(y.shape)
# torch.Size([6, 4])
```

## 常见用法

### 展平为二维

在神经网络中，经常将卷积输出展平后送入全连接层：

```python
x = torch.randn(32, 3, 28, 28)

# batch_size 保持不变，其余维度展平
y = x.view(32, -1)

print(y.shape)
# torch.Size([32, 2352])
```

其中 `-1` 表示让 PyTorch 自动推断该维度大小。

---

## 适用场景

适合用于：

1. **展平 Tensor**
2. **改变张量形状**
3. **CNN 输出接入 Linear 层**
4. **将一维数据恢复成多维结构**

---

## 注意事项

`view()` 要求 Tensor 在内存中是 **连续的 contiguous**。

例如：

```python
x = torch.randn(2, 3)
y = x.transpose(0, 1)

z = y.view(6)
```

这可能会报错：

```text
RuntimeError: view size is not compatible with input tensor's size and stride
```

因为 `transpose()` 后 Tensor 可能不再连续。

解决方式：

```python
z = y.contiguous().view(6)
```

或者直接使用：

```python
z = y.reshape(6)
```

---

# 2. `reshape`：更灵活的形状变换

## 作用

`reshape()` 与 `view()` 类似，也用于改变 Tensor 形状。

```python
x = torch.randn(2, 3, 4)
y = x.reshape(6, 4)
```

## 与 `view` 的区别

| 对比项 | `view` | `reshape` |
|---|---|---|
| 是否要求内存连续 | **要求连续** | 不一定 |
| 是否可能复制数据 | 通常不复制 | 可能复制 |
| 使用便利性 | 较严格 | 更灵活 |
| 性能可控性 | 更明确 | 可能有隐式开销 |

## 推荐理解

- 如果 Tensor 是连续的，`reshape()` 通常等价于 `view()`。
- 如果 Tensor 不连续，`reshape()` 可能会自动复制一份新的连续数据。

---

## 适用场景

适合用于：

1. **不确定 Tensor 是否连续时改变形状**
2. **快速调整维度**
3. **替代 `view()` 避免 contiguous 报错**

示例：

```python
x = torch.randn(2, 3)
y = x.transpose(0, 1)

z = y.reshape(6)
```

---

# 3. `transpose`：交换两个维度

## 作用

`transpose(dim0, dim1)` 用于交换 Tensor 的两个维度。

```python
x = torch.randn(2, 3)

y = x.transpose(0, 1)

print(y.shape)
# torch.Size([3, 2])
```

---

## 高维示例

```python
x = torch.randn(32, 3, 224, 224)

# 交换 channel 和 height 维度
y = x.transpose(1, 2)

print(y.shape)
# torch.Size([32, 224, 3, 224])
```

---

## 适用场景

适合用于：

1. **矩阵转置**
2. **交换特定两个维度**
3. **注意力机制中调整 Q/K/V 维度**
4. **处理 RNN、Transformer 中的 batch 和 sequence 维度**

例如，Transformer 中常见：

```python
x = torch.randn(32, 128, 768)
# [batch, seq_len, hidden]

y = x.transpose(0, 1)
# [seq_len, batch, hidden]
```

---

## 注意事项

`transpose()` 返回的 Tensor 通常是 **非连续的视图**，后续如果要使用 `view()`，通常需要：

```python
y = x.transpose(0, 1).contiguous().view(...)
```

---

# 4. `permute`：任意维度重排

## 作用

`permute()` 可以按照指定顺序重新排列所有维度。

```python
x = torch.randn(2, 3, 4)

y = x.permute(2, 0, 1)

print(y.shape)
# torch.Size([4, 2, 3])
```

原始维度：

```text
x: [dim0, dim1, dim2] = [2, 3, 4]
```

执行：

```python
x.permute(2, 0, 1)
```

结果：

```text
[dim2, dim0, dim1] = [4, 2, 3]
```

---

## 与 `transpose` 的区别

| 操作 | 功能 |
|---|---|
| `transpose` | 只能交换两个维度 |
| `permute` | 可以任意重排多个维度 |

---

## 适用场景

### 图像格式转换

PyTorch 图像通常使用：

```text
[N, C, H, W]
```

而有些图像库如 NumPy、OpenCV、Matplotlib 常用：

```text
[N, H, W, C]
```

转换示例：

```python
x = torch.randn(32, 3, 224, 224)

y = x.permute(0, 2, 3, 1)

print(y.shape)
# torch.Size([32, 224, 224, 3])
```

---

## 注意事项

和 `transpose()` 一样，`permute()` 返回的 Tensor 也通常是 **非连续的**。

```python
y = x.permute(0, 2, 3, 1)

z = y.contiguous().view(32, -1)
```

---

# 5. `squeeze`：删除大小为 1 的维度

## 作用

`squeeze()` 用于删除 Tensor 中维度大小为 `1` 的轴。

```python
x = torch.randn(1, 3, 1, 5)

y = x.squeeze()

print(y.shape)
# torch.Size([3, 5])
```

---

## 指定维度删除

```python
x = torch.randn(1, 3, 1, 5)

y = x.squeeze(0)

print(y.shape)
# torch.Size([3, 1, 5])
```

如果指定维度的大小不是 `1`，则不会改变：

```python
x = torch.randn(2, 3)

y = x.squeeze(0)

print(y.shape)
# torch.Size([2, 3])
```

---

## 适用场景

适合用于：

1. **去除多余的 batch 维度**
2. **模型输出后处理**
3. **标签维度调整**
4. **从 `[B, 1]` 转为 `[B]`**

示例：

```python
logits = torch.randn(32, 1)

pred = logits.squeeze(1)

print(pred.shape)
# torch.Size([32])
```

---

## 注意事项

谨慎使用无参数的 `squeeze()`：

```python
x = torch.randn(1, 3, 224, 224)
y = x.squeeze()
```

如果 batch size 为 `1`，会把 batch 维度也删除，可能导致后续模型维度错误。

更推荐：

```python
y = x.squeeze(dim=1)
```

---

# 6. `unsqueeze`：增加一个大小为 1 的维度

## 作用

`unsqueeze(dim)` 在指定位置插入一个大小为 `1` 的维度。

```python
x = torch.randn(3, 4)

y = x.unsqueeze(0)

print(y.shape)
# torch.Size([1, 3, 4])
```

---

## 示例

```python
x = torch.randn(3)

x1 = x.unsqueeze(0)
x2 = x.unsqueeze(1)

print(x1.shape)
# torch.Size([1, 3])

print(x2.shape)
# torch.Size([3, 1])
```

---

## 适用场景

适合用于：

1. **增加 batch 维度**
2. **增加 channel 维度**
3. **配合 broadcasting**
4. **图像、文本、语音输入模型前的维度调整**

例如单张灰度图：

```python
x = torch.randn(28, 28)

# 增加 batch 和 channel 维度
x = x.unsqueeze(0).unsqueeze(0)

print(x.shape)
# torch.Size([1, 1, 28, 28])
```

---

# 7. `flatten`：展平指定维度

## 作用

`flatten()` 用于将连续的一段维度展平成一个维度。

```python
x = torch.randn(2, 3, 4)

y = torch.flatten(x)

print(y.shape)
# torch.Size([24])
```

---

## 保留 batch 维度

```python
x = torch.randn(32, 3, 28, 28)

y = torch.flatten(x, start_dim=1)

print(y.shape)
# torch.Size([32, 2352])
```

---

## 适用场景

适合用于：

1. **CNN 特征送入全连接层**
2. **多维特征展平**
3. **比 `view(batch, -1)` 可读性更好**

推荐写法：

```python
x = torch.flatten(x, start_dim=1)
```

而不是：

```python
x = x.view(x.size(0), -1)
```

前者语义更清晰。

---

# 8. `contiguous`：让 Tensor 内存连续

## 作用

`contiguous()` 会返回一个内存连续的 Tensor。

```python
x = torch.randn(2, 3)
y = x.transpose(0, 1)

print(y.is_contiguous())
# False

z = y.contiguous()

print(z.is_contiguous())
# True
```

---

## 适用场景

适合用于：

1. **`transpose` / `permute` 后接 `view`**
2. **需要连续内存的底层算子**
3. **提高部分操作的内存访问效率**

示例：

```python
x = torch.randn(32, 3, 224, 224)

y = x.permute(0, 2, 3, 1)

z = y.contiguous().view(32, -1)
```

---

## 注意事项

`contiguous()` 可能会触发 **数据复制**，因此在大 Tensor 上需要注意内存和性能开销。

---

# 9. `expand`：广播式扩展，不复制数据

## 作用

`expand()` 用于将大小为 `1` 的维度扩展到更大的尺寸，通常不复制底层数据。

```python
x = torch.randn(1, 3)

y = x.expand(4, 3)

print(y.shape)
# torch.Size([4, 3])
```

---

## 示例

```python
bias = torch.randn(1, 3)

x = bias.expand(32, 3)
```

---

## 适用场景

适合用于：

1. **广播 bias**
2. **构造重复视图**
3. **节省内存**
4. **需要将 `[1, C]` 扩展为 `[B, C]`**

---

## 注意事项

只能扩展原本大小为 `1` 的维度：

```python
x = torch.randn(2, 3)

y = x.expand(4, 3)
```

这会报错，因为第一维从 `2` 扩展到 `4` 不符合广播规则。

此外，`expand()` 不复制数据，多个位置可能共享同一块内存，不适合直接做原地修改。

---

# 10. `repeat`：真实复制数据

## 作用

`repeat()` 会按照指定次数复制 Tensor。

```python
x = torch.tensor([[1, 2, 3]])

y = x.repeat(4, 1)

print(y)
```

输出：

```text
tensor([[1, 2, 3],
        [1, 2, 3],
        [1, 2, 3],
        [1, 2, 3]])
```

---

## 与 `expand` 的区别

| 对比项 | `expand` | `repeat` |
|---|---|---|
| 是否复制数据 | **不复制** | **复制** |
| 内存开销 | 小 | 大 |
| 维度限制 | 只能扩展 size=1 的维度 | 更灵活 |
| 是否适合原地修改 | 不推荐 | 可以 |

---

## 适用场景

适合用于：

1. **需要真实复制数据**
2. **后续要对不同副本分别修改**
3. **构造重复样本**
4. **无法用广播表达的重复操作**

---

# 11. `cat`：沿已有维度拼接

## 作用

`torch.cat()` 用于沿着一个已有维度拼接多个 Tensor。

```python
x1 = torch.randn(2, 3)
x2 = torch.randn(2, 5)

y = torch.cat([x1, x2], dim=1)

print(y.shape)
# torch.Size([2, 8])
```

---

## 要求

除了拼接维度外，其他维度必须一致。

```python
x1: [2, 3]
x2: [2, 5]
cat dim=1 -> [2, 8]
```

---

## 适用场景

适合用于：

1. **特征拼接**
2. **多模态特征融合**
3. **不同分支网络输出合并**
4. **batch 维度合并**

示例：

```python
image_feat = torch.randn(32, 512)
text_feat = torch.randn(32, 768)

feat = torch.cat([image_feat, text_feat], dim=1)

print(feat.shape)
# torch.Size([32, 1280])
```

---

# 12. `stack`：新增维度后堆叠

## 作用

`torch.stack()` 会在新维度上堆叠多个 Tensor。

```python
x1 = torch.randn(3, 4)
x2 = torch.randn(3, 4)

y = torch.stack([x1, x2], dim=0)

print(y.shape)
# torch.Size([2, 3, 4])
```

---

## 与 `cat` 的区别

| 操作 | 是否新增维度 | 示例 |
|---|---|---|
| `cat` | 不新增维度 | `[3, 4] + [3, 4] -> [6, 4]` |
| `stack` | 新增维度 | `[3, 4] + [3, 4] -> [2, 3, 4]` |

---

## 示例对比

```python
x1 = torch.randn(3, 4)
x2 = torch.randn(3, 4)

a = torch.cat([x1, x2], dim=0)
b = torch.stack([x1, x2], dim=0)

print(a.shape)
# torch.Size([6, 4])

print(b.shape)
# torch.Size([2, 3, 4])
```

---

## 适用场景

适合用于：

1. **将多个样本组成 batch**
2. **将多个时间步组成序列**
3. **将多个模型输出组织成 ensemble 维度**
4. **保留每个 Tensor 的独立结构**

---

# 13. `split`：按指定大小拆分

## 作用

`split()` 将 Tensor 沿指定维度拆成多个块。

```python
x = torch.randn(10, 3)

parts = torch.split(x, 4, dim=0)

for p in parts:
    print(p.shape)
```

输出：

```text
torch.Size([4, 3])
torch.Size([4, 3])
torch.Size([2, 3])
```

---

## 指定每块大小

```python
x = torch.randn(10, 3)

a, b, c = torch.split(x, [2, 3, 5], dim=0)
```

---

## 适用场景

适合用于：

1. **拆分 batch**
2. **拆分模型输出**
3. **多任务学习中拆分不同 head 的输出**
4. **Transformer 中拆分 Q、K、V**

示例：

```python
qkv = torch.randn(32, 128, 768 * 3)

q, k, v = torch.split(qkv, 768, dim=-1)
```

---

# 14. `chunk`：按块数拆分

## 作用

`chunk()` 按指定的块数拆分 Tensor。

```python
x = torch.randn(10, 3)

parts = torch.chunk(x, 3, dim=0)

for p in parts:
    print(p.shape)
```

可能输出：

```text
torch.Size([4, 3])
torch.Size([4, 3])
torch.Size([2, 3])
```

---

## 与 `split` 的区别

| 操作 | 拆分方式 |
|---|---|
| `split` | 指定每块大小 |
| `chunk` | 指定拆成几块 |

---

## 适用场景

适合用于：

1. **多 GPU / 多进程数据拆分**
2. **将 batch 均分**
3. **将特征分成若干组**

---

# 15. `unbind`：移除某个维度并拆成多个 Tensor

## 作用

`unbind()` 沿指定维度拆分，并移除该维度。

```python
x = torch.randn(3, 2, 4)

a, b, c = torch.unbind(x, dim=0)

print(a.shape)
# torch.Size([2, 4])
```

---

## 适用场景

适合用于：

1. **逐时间步处理序列**
2. **逐样本处理 batch**
3. **拆出多个分支输出**

---

# 16. `index_select`：按索引选择数据

## 作用

`index_select()` 根据给定索引选择指定维度的数据。

```python
x = torch.randn(5, 3)

idx = torch.tensor([0, 2, 4])

y = torch.index_select(x, dim=0, index=idx)

print(y.shape)
# torch.Size([3, 3])
```

---

## 适用场景

适合用于：

1. **选择部分样本**
2. **选择部分特征**
3. **根据索引筛选 embedding**
4. **从 batch 中抽取指定元素**

---

# 17. `gather`：按索引收集元素

## 作用

`gather()` 可以按照索引 Tensor 从指定维度收集元素。

```python
x = torch.tensor([[10, 20, 30],
                  [40, 50, 60]])

idx = torch.tensor([[0, 2],
                    [1, 0]])

y = torch.gather(x, dim=1, index=idx)

print(y)
```

输出：

```text
tensor([[10, 30],
        [50, 40]])
```

---

## 适用场景

适合用于：

1. **分类任务中取目标类别的概率**
2. **Top-k 后取对应值**
3. **强化学习中取 action 对应的 Q 值**

示例：

```python
logits = torch.randn(32, 10)
labels = torch.randint(0, 10, (32, 1))

selected = torch.gather(logits, dim=1, index=labels)

print(selected.shape)
# torch.Size([32, 1])
```

---

# 18. `movedim`：移动维度到指定位置

## 作用

`movedim(input, source, destination)` 将某些维度移动到目标位置。

```python
x = torch.randn(2, 3, 4, 5)

y = torch.movedim(x, 1, -1)

print(y.shape)
# torch.Size([2, 4, 5, 3])
```

---

## 适用场景

适合用于：

1. **将 channel 维移到最后**
2. **比 `permute` 更关注“移动某个维度”时语义更清晰**
3. **图像格式转换**

示例：

```python
x = torch.randn(32, 3, 224, 224)

y = torch.movedim(x, 1, -1)

print(y.shape)
# torch.Size([32, 224, 224, 3])
```

---

# 19. `to` / 类型转换 / 设备转换

虽然这类操作不改变形状，但在实际数据处理和模型训练中非常常见。

## 作用

用于改变 Tensor 的：

1. **数据类型**
2. **设备位置**
3. **内存格式**

```python
x = torch.randn(3, 4)

x = x.to(torch.float16)
x = x.to("cuda")
```

---

## 常见写法

```python
x = x.float()
x = x.long()
x = x.cuda()
x = x.cpu()
x = x.to(device)
```

---

## 适用场景

| 操作 | 场景 |
|---|---|
| `float()` | 输入模型前转为浮点 |
| `long()` | 分类标签用于 `CrossEntropyLoss` |
| `cuda()` | 放到 GPU |
| `cpu()` | 转 NumPy 或保存 |
| `to(device)` | 统一设备管理 |

示例：

```python
device = torch.device("cuda" if torch.cuda.is_available() else "cpu")

x = x.to(device)
model = model.to(device)
```

---

# 20. Broadcasting：自动广播变换

## 作用

PyTorch 支持 NumPy 风格的广播机制。不同形状的 Tensor 在计算时可以自动扩展。

```python
x = torch.randn(32, 10)
bias = torch.randn(10)

y = x + bias

print(y.shape)
# torch.Size([32, 10])
```

这里 `bias` 会自动从 `[10]` 广播为 `[32, 10]`。

---

## 适用场景

适合用于：

1. **加 bias**
2. **归一化**
3. **按通道缩放**
4. **矩阵批量计算**

示例：

```python
x = torch.randn(32, 3, 224, 224)

mean = torch.tensor([0.485, 0.456, 0.406]).view(1, 3, 1, 1)
std = torch.tensor([0.229, 0.224, 0.225]).view(1, 3, 1, 1)

y = (x - mean) / std
```

---

# 典型场景示例

## 场景一：CNN 输出接全连接层

```python
import torch
import torch.nn as nn

x = torch.randn(32, 64, 7, 7)

# 推荐方式
x = torch.flatten(x, start_dim=1)

fc = nn.Linear(64 * 7 * 7, 10)
out = fc(x)

print(out.shape)
# torch.Size([32, 10])
```

核心操作：

```python
torch.flatten(x, start_dim=1)
```

---

## 场景二：图像从 PyTorch 格式转为可视化格式

```python
x = torch.randn(3, 224, 224)

# [C, H, W] -> [H, W, C]
img = x.permute(1, 2, 0)

print(img.shape)
# torch.Size([224, 224, 3])
```

如果要转 NumPy：

```python
img_np = img.cpu().numpy()
```

---

## 场景三：Transformer 中调整维度

```python
x = torch.randn(32, 128, 768)
# [batch, seq_len, hidden]

x = x.transpose(0, 1)
# [seq_len, batch, hidden]
```

多头注意力中常见：

```python
x = torch.randn(32, 128, 768)

num_heads = 12
head_dim = 64

x = x.view(32, 128, num_heads, head_dim)
x = x.transpose(1, 2)

print(x.shape)
# torch.Size([32, 12, 128, 64])
```

---

## 场景四：分类任务中标签维度调整

```python
logits = torch.randn(32, 10)
labels = torch.randint(0, 10, (32, 1))

labels = labels.squeeze(1)

print(labels.shape)
# torch.Size([32])
```

`CrossEntropyLoss` 通常要求：

```text
logits: [batch_size, num_classes]
labels: [batch_size]
```

---

# 常见操作速查表

| 操作 | 示例 | 结果形状 | 主要用途 |
|---|---|---|---|
| `view` | `x.view(6, 4)` | 改变形状 | 高效 reshape，要求连续 |
| `reshape` | `x.reshape(6, 4)` | 改变形状 | 更灵活的 reshape |
| `flatten` | `x.flatten(1)` | 展平部分维度 | CNN 接 Linear |
| `squeeze` | `x.squeeze(1)` | 删除 size=1 维度 | 去掉多余维度 |
| `unsqueeze` | `x.unsqueeze(0)` | 增加 size=1 维度 | 增加 batch/channel |
| `transpose` | `x.transpose(1, 2)` | 交换两个维度 | 矩阵转置、序列维度调整 |
| `permute` | `x.permute(0, 2, 3, 1)` | 任意维度重排 | NCHW 与 NHWC 转换 |
| `movedim` | `torch.movedim(x, 1, -1)` | 移动某维 | 更语义化的维度移动 |
| `contiguous` | `x.contiguous()` | 内存连续 | `permute` 后接 `view` |
| `expand` | `x.expand(32, 3)` | 广播扩展 | 节省内存 |
| `repeat` | `x.repeat(32, 1)` | 复制扩展 | 真实复制数据 |
| `cat` | `torch.cat(xs, dim=1)` | 沿已有维度拼接 | 特征融合 |
| `stack` | `torch.stack(xs, dim=0)` | 新增维度堆叠 | 构造 batch |
| `split` | `torch.split(x, 4, dim=0)` | 按大小拆分 | 拆 batch/特征 |
| `chunk` | `torch.chunk(x, 3, dim=0)` | 按块数拆分 | 均分 Tensor |
| `unbind` | `torch.unbind(x, dim=0)` | 移除维度后拆分 | 逐样本/逐时间步处理 |
| `gather` | `torch.gather(x, 1, idx)` | 按索引取值 | 取类别概率/Q 值 |
| `index_select` | `torch.index_select(x, 0, idx)` | 按索引选行/列 | 样本或特征筛选 |

---

# 实践建议

## 1. 改形状优先考虑 `reshape` 或 `flatten`

如果只是简单改变形状：

```python
x = x.reshape(batch_size, -1)
```

如果是 CNN 展平：

```python
x = torch.flatten(x, start_dim=1)
```

---

## 2. 交换两个维度用 `transpose`

```python
x = x.transpose(1, 2)
```

适合：

```text
[B, L, C] -> [B, C, L]
```

---

## 3. 多维重排用 `permute`

```python
x = x.permute(0, 2, 3, 1)
```

适合：

```text
[N, C, H, W] -> [N, H, W, C]
```

---

## 4. `permute` / `transpose` 后接 `view` 要小心

推荐写法：

```python
x = x.transpose(1, 2).contiguous().view(...)
```

或者：

```python
x = x.transpose(1, 2).reshape(...)
```

---

## 5. 扩展数据时优先考虑 `expand`

如果只是广播，不需要真实复制：

```python
x = x.expand(batch_size, -1)
```

如果需要每份数据独立存在：

```python
x = x.repeat(batch_size, 1)
```

---

# 核心总结

- **`view` / `reshape`**：改变形状。
- **`flatten`**：展平维度，CNN 中常用。
- **`transpose`**：交换两个维度。
- **`permute`**：任意重排多个维度。
- **`squeeze` / `unsqueeze`**：删除或增加大小为 `1` 的维度。
- **`contiguous`**：解决非连续 Tensor 后续 `view` 报错问题。
- **`expand` / `repeat`**：扩展数据，前者不复制，后者复制。
- **`cat` / `stack`**：拼接数据，前者沿已有维度，后者新增维度。
- **`split` / `chunk` / `unbind`**：拆分 Tensor。
- **`gather` / `index_select`**：按索引选择数据。

如果只记最常用的几组，可以优先掌握：

```text
view / reshape / flatten
transpose / permute
squeeze / unsqueeze
contiguous
cat / stack
expand / repeat
```
