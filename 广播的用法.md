# 广播（Broadcasting）规则

**广播（Broadcasting）**：当两个张量形状不同时，框架会自动扩展较小的张量，使其能够进行**逐元素运算**。

---

## 三条核心规则

1. **从最右边维度开始对齐**
2. **某个维度为 `1` 时，可以扩展为任意大小**
3. **缺失的维度视为 `1`，也可以扩展**

---

## Attention 代码中的广播示例

### 例 1：`unsqueeze` 的作用

```python
candidate_emb: [B, D]
```

无法直接与下面的张量进行逐元素运算：

```python
seq_emb: [B, L, D]
```

需要通过 `unsqueeze(1)` 增加一个维度：

```python
candidate_emb.unsqueeze(1): [B, 1, D]
```

然后进行广播：

```python
[B, 1, D] → [B, L, D]
```

其中，维度 `1` 被扩展为 `L`，相当于把每个 `candidate` 复制了 `L` 份。

---

### 例 2：加权求和

```python
weight: [B, L]
seq_emb: [B, L, D]
```

为了让 `weight` 能够作用到 `seq_emb` 的最后一维，需要增加一个维度：

```python
weight.unsqueeze(-1): [B, L, 1]
```

然后与 `seq_emb` 相乘：

```python
[B, L, 1] * [B, L, D] → [B, L, D]
```

其中，最后一维的 `1` 被扩展为 `D`。

也就是说：

> 每个 `weight` 标量会乘到对应位置上 `D` 维向量的所有元素。

---

### 例 3：标量广播

```python
a: [B, L, D]
b: 5
```

标量 `5` 的形状可以看作：

```python
[]
```

在运算时会被广播为：

```python
[B, L, D]
```

因此：

```python
a * 5
```

等价于：

> `a` 中的每个元素都乘以 `5`。

---

## 常见广播模式速查

| 张量 A | 张量 B | 结果 | 说明 |
|---|---|---|---|
| `[B, L, D]` | `[D]` | `[B, L, D]` | 最右对齐，`D` 匹配 |
| `[B, L, D]` | `[1, D]` | `[B, L, D]` | `1` 扩展为 `L` |
| `[B, L, D]` | `[L, 1]` | `[B, L, D]` | 缺失维度补 `1`，两端各自扩展 |
| `[B, L, D]` | `[1, L, 1]` | `[B, L, D]` | 两处 `1` 都扩展 |

---

## 不能广播的例子

```python
a: [B, L, D]
b: [B, D, L]
```

从最右边维度开始对齐：

```text
a: [B, L, D]
b: [B, D, L]
```

最右维度为：

```text
D vs L
```

如果 `D != L`，且二者都不为 `1`，则无法广播。

因此：

```python
[B, L, D] 和 [B, D, L] 不能广播
```

---

## 一句话总结

**广播就是：维度为 `1` 就复制，缺失维度就补 `1` 再复制，非 `1` 维度必须完全一致。**
