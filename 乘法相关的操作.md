## PyTorch 中常见“乘法相关操作”总览

PyTorch 里的乘法可以大致分为几类：

1. **逐元素乘法**：`*`、`torch.mul`
2. **矩阵 / 向量乘法**：`torch.matmul`、`@`、`torch.mm`、`torch.mv`、`torch.dot`
3. **批量矩阵乘法**：`torch.bmm`
4. **高维张量乘法 / 广义乘法**：`torch.einsum`、`torch.tensordot`
5. **外积 / Kronecker 积等特殊乘法**：`torch.outer`、`torch.kron`
6. **融合型乘加操作**：`torch.addmm`、`torch.baddbmm`、`torch.addbmm`

下面按使用频率和场景展开说明。

---

# 1. `*`：逐元素乘法

## 用法

```python
import torch

a = torch.tensor([1, 2, 3])
b = torch.tensor([4, 5, 6])

c = a * b
print(c)
# tensor([ 4, 10, 18])
```

`*` 是 **element-wise multiplication**，即逐元素相乘。

---

## 支持广播机制

```python
a = torch.randn(2, 3)
b = torch.randn(3)

c = a * b
print(c.shape)
# torch.Size([2, 3])
```

这里 `b` 会被广播成 `(2, 3)` 后与 `a` 逐元素相乘。

---

## 常见使用场景

### 1. 特征加权

```python
features = torch.randn(32, 128)
weights = torch.randn(128)

out = features * weights
```

常用于：

- 通道加权
- attention mask
- 特征缩放
- loss weighting

---

### 2. Mask 操作

```python
x = torch.randn(4, 5)
mask = torch.tensor([[1, 1, 0, 0, 1]]).bool()

y = x * mask
```

常用于：

- NLP 中 padding mask
- 图像区域 mask
- attention mask
- 忽略无效位置

---

### 3. 标量缩放

```python
x = torch.randn(10)
y = x * 0.5
```

常用于：

- 学习率缩放
- loss 权重调整
- 数据归一化

---

# 2. `torch.mul`：逐元素乘法函数形式

## 用法

```python
a = torch.tensor([1, 2, 3])
b = torch.tensor([4, 5, 6])

c = torch.mul(a, b)
print(c)
# tensor([ 4, 10, 18])
```

`torch.mul(a, b)` 与 `a * b` 基本等价。

---

## 支持标量

```python
x = torch.tensor([1., 2., 3.])
y = torch.mul(x, 10)

print(y)
# tensor([10., 20., 30.])
```

---

## 支持 `out` 参数

```python
a = torch.tensor([1, 2, 3])
b = torch.tensor([4, 5, 6])
out = torch.empty(3, dtype=torch.int64)

torch.mul(a, b, out=out)

print(out)
# tensor([ 4, 10, 18])
```

---

## 使用场景

与 `*` 相同，但在以下场景更常见：

- 需要函数式 API
- 写通用算子封装
- 使用 `out` 参数减少内存分配
- 与其他 `torch.xxx` API 风格保持一致

---

# 3. `@`：矩阵乘法运算符

## 用法

```python
a = torch.randn(2, 3)
b = torch.randn(3, 4)

c = a @ b
print(c.shape)
# torch.Size([2, 4])
```

`@` 是 Python 的矩阵乘法运算符，在 PyTorch 中通常等价于调用 `torch.matmul(a, b)`。

---

## 使用场景

### 1. 线性层计算

```python
x = torch.randn(32, 128)
w = torch.randn(128, 64)

y = x @ w
print(y.shape)
# torch.Size([32, 64])
```

对应神经网络中：

```text
y = xW
```

---

### 2. Attention 计算

```python
Q = torch.randn(8, 16, 64)
K = torch.randn(8, 16, 64)

scores = Q @ K.transpose(-2, -1)
print(scores.shape)
# torch.Size([8, 16, 16])
```

用于 Transformer 中的：

```text
Attention Score = QK^T
```

---

# 4. `torch.matmul`：通用矩阵乘法

`torch.matmul` 是 PyTorch 中最常用的矩阵乘法接口之一，支持：

- 1D 向量
- 2D 矩阵
- 高维 batch 矩阵
- 广播 batch 维度

---

## 用法一：向量 dot product

```python
a = torch.randn(3)
b = torch.randn(3)

c = torch.matmul(a, b)
print(c.shape)
# torch.Size([])
```

相当于两个向量点积，输出标量。

---

## 用法二：矩阵乘矩阵

```python
a = torch.randn(2, 3)
b = torch.randn(3, 4)

c = torch.matmul(a, b)
print(c.shape)
# torch.Size([2, 4])
```

---

## 用法三：批量矩阵乘法

```python
a = torch.randn(10, 2, 3)
b = torch.randn(10, 3, 4)

c = torch.matmul(a, b)
print(c.shape)
# torch.Size([10, 2, 4])
```

---

## 用法四：支持广播 batch 维度

```python
a = torch.randn(10, 2, 3)
b = torch.randn(3, 4)

c = torch.matmul(a, b)
print(c.shape)
# torch.Size([10, 2, 4])
```

这里 `b` 会广播到每个 batch 上。

---

## 使用场景

`torch.matmul` 适合大多数矩阵乘法场景：

- 普通矩阵乘法
- 批量矩阵乘法
- Transformer attention
- 图神经网络邻接矩阵乘法
- 多 batch 特征映射
- 高维张量最后两个维度的矩阵乘法

---

# 5. `torch.mm`：二维矩阵乘法

## 用法

```python
a = torch.randn(2, 3)
b = torch.randn(3, 4)

c = torch.mm(a, b)
print(c.shape)
# torch.Size([2, 4])
```

要求：

```text
a: 2D tensor, shape = (m, n)
b: 2D tensor, shape = (n, p)
out: shape = (m, p)
```

---

## 与 `matmul` 的区别

| 操作 | 支持维度 | 支持广播 | 典型用途 |
|---|---:|---:|---|
| `torch.mm` | 只支持 2D | 不支持 | 明确的二维矩阵乘法 |
| `torch.matmul` | 支持 1D、2D、高维 | 支持 | 通用矩阵乘法 |

---

## 使用场景

适合输入明确是二维矩阵的场景：

```python
x = torch.randn(64, 128)
w = torch.randn(128, 256)

y = torch.mm(x, w)
```

常用于：

- 手写全连接层
- embedding 后接线性投影
- 二维特征变换

---

# 6. `torch.bmm`：批量矩阵乘法

## 用法

```python
a = torch.randn(10, 2, 3)
b = torch.randn(10, 3, 4)

c = torch.bmm(a, b)
print(c.shape)
# torch.Size([10, 2, 4])
```

要求：

```text
a: shape = (batch, m, n)
b: shape = (batch, n, p)
out: shape = (batch, m, p)
```

---

## 注意点

`torch.bmm` **只支持 3D Tensor**，并且 **不支持广播**。

例如下面会报错：

```python
a = torch.randn(10, 2, 3)
b = torch.randn(3, 4)

c = torch.bmm(a, b)
```

因为 `b` 不是三维。

如果需要广播，应该使用：

```python
c = torch.matmul(a, b)
```

---

## 使用场景

### 1. 批量线性变换

```python
x = torch.randn(32, 10, 64)
w = torch.randn(32, 64, 128)

y = torch.bmm(x, w)
print(y.shape)
# torch.Size([32, 10, 128])
```

每个 batch 使用不同的权重矩阵。

---

### 2. Attention 分数计算

```python
Q = torch.randn(32, 20, 64)
K = torch.randn(32, 20, 64)

scores = torch.bmm(Q, K.transpose(1, 2))
print(scores.shape)
# torch.Size([32, 20, 20])
```

适合单头 attention 或处理已经合并 head 维度后的 attention。

---

### 3. 图像 / 点云中的批量坐标变换

```python
points = torch.randn(16, 1000, 3)
transform = torch.randn(16, 3, 3)

out = torch.bmm(points, transform)
print(out.shape)
# torch.Size([16, 1000, 3])
```

常用于：

- 3D 点云旋转
- 图像仿射变换
- batch 内每个样本使用不同变换矩阵

---

# 7. `torch.mv`：矩阵乘向量

## 用法

```python
matrix = torch.randn(2, 3)
vector = torch.randn(3)

out = torch.mv(matrix, vector)
print(out.shape)
# torch.Size([2])
```

要求：

```text
matrix: shape = (m, n)
vector: shape = (n,)
out: shape = (m,)
```

---

## 使用场景

适合明确是矩阵乘向量的场景：

- 线性代数计算
- 单样本特征投影
- 权重矩阵作用到单个向量
- 推荐系统中单个用户向量打分

示例：

```python
user = torch.randn(128)
item_matrix = torch.randn(1000, 128)

scores = torch.mv(item_matrix, user)
print(scores.shape)
# torch.Size([1000])
```

表示一个用户与 1000 个 item 的线性打分。

---

# 8. `torch.dot`：一维向量点积

## 用法

```python
a = torch.tensor([1., 2., 3.])
b = torch.tensor([4., 5., 6.])

c = torch.dot(a, b)
print(c)
# tensor(32.)
```

计算：

```text
1*4 + 2*5 + 3*6 = 32
```

---

## 要求

`torch.dot` 只支持 **一维 Tensor**。

```python
a = torch.randn(3)
b = torch.randn(3)

torch.dot(a, b)
```

---

## 使用场景

- 计算两个向量相似度
- 余弦相似度的分子部分
- 单个 query 和 key 的相关性
- 线性打分函数

例如：

```python
q = torch.randn(128)
k = torch.randn(128)

score = torch.dot(q, k)
```

---

# 9. `torch.outer`：向量外积

## 用法

```python
a = torch.tensor([1, 2, 3])
b = torch.tensor([4, 5])

c = torch.outer(a, b)
print(c)
```

输出：

```text
tensor([[ 4,  5],
        [ 8, 10],
        [12, 15]])
```

形状：

```text
a: shape = (m,)
b: shape = (n,)
out: shape = (m, n)
```

---

## 使用场景

- 构造低秩矩阵
- 统计特征交叉
- 双线性模型
- 协方差矩阵近似
- 推荐系统中的交叉特征建模

示例：

```python
user = torch.randn(64)
item = torch.randn(32)

cross_feature = torch.outer(user, item)
print(cross_feature.shape)
# torch.Size([64, 32])
```

---

# 10. `torch.einsum`：爱因斯坦求和约定

`torch.einsum` 是非常灵活的张量乘法和求和工具，可以表达：

- 点积
- 矩阵乘法
- 批量矩阵乘法
- 转置
- 求和
- attention
- 高维张量收缩

---

## 用法一：逐元素乘法

```python
a = torch.randn(3)
b = torch.randn(3)

c = torch.einsum('i,i->i', a, b)
print(c.shape)
# torch.Size([3])
```

---

## 用法二：点积

```python
a = torch.randn(3)
b = torch.randn(3)

c = torch.einsum('i,i->', a, b)
print(c.shape)
# torch.Size([])
```

---

## 用法三：矩阵乘法

```python
a = torch.randn(2, 3)
b = torch.randn(3, 4)

c = torch.einsum('ik,kj->ij', a, b)
print(c.shape)
# torch.Size([2, 4])
```

---

## 用法四：批量矩阵乘法

```python
a = torch.randn(10, 2, 3)
b = torch.randn(10, 3, 4)

c = torch.einsum('bij,bjk->bik', a, b)
print(c.shape)
# torch.Size([10, 2, 4])
```

---

## 用法五：Multi-Head Attention

```python
Q = torch.randn(32, 8, 128, 64)
K = torch.randn(32, 8, 128, 64)

scores = torch.einsum('bhqd,bhkd->bhqk', Q, K)
print(scores.shape)
# torch.Size([32, 8, 128, 128])
```

含义：

```text
b: batch
h: head
q: query length
k: key length
d: hidden dimension
```

---

## 使用场景

`einsum` 适合：

- 张量维度较复杂
- 希望显式表达维度语义
- 多头 attention
- 双线性模型
- 高阶特征交互
- 复杂 contraction 操作

缺点是：

- 对初学者可读性较低
- 写错下标时不容易发现
- 某些情况下性能不如专门算子

---

# 11. `torch.tensordot`：指定维度收缩乘法

## 用法

```python
a = torch.randn(2, 3, 4)
b = torch.randn(4, 3, 5)

c = torch.tensordot(a, b, dims=([2, 1], [0, 1]))

print(c.shape)
# torch.Size([2, 5])
```

这里表示：

- `a` 的第 `2`、`1` 维，即大小为 `4`、`3`
- 与 `b` 的第 `0`、`1` 维，即大小为 `4`、`3`
- 对这两个维度进行乘法求和

---

## 简单矩阵乘法示例

```python
a = torch.randn(2, 3)
b = torch.randn(3, 4)

c = torch.tensordot(a, b, dims=1)

print(c.shape)
# torch.Size([2, 4])
```

等价于：

```python
torch.matmul(a, b)
```

---

## 使用场景

适合：

- 高维张量 contraction
- 科学计算
- 多维数组运算
- 张量网络
- 需要明确指定哪些维度相乘求和

如果只是普通矩阵乘法，优先用 `matmul` 或 `mm`。

---

# 12. `torch.kron`：Kronecker 积

## 用法

```python
a = torch.tensor([[1, 2],
                  [3, 4]])

b = torch.tensor([[0, 5],
                  [6, 7]])

c = torch.kron(a, b)
print(c)
```

输出形状：

```text
a.shape = (2, 2)
b.shape = (2, 2)
c.shape = (4, 4)
```

每个 `a[i, j]` 会乘以整个矩阵 `b`。

---

## 使用场景

- 线性代数
- 信号处理
- 量子计算
- 图模型
- 结构化矩阵构造
- 某些高效参数化模型

一般深度学习日常开发中使用频率较低。

---

# 13. `torch.addmm`：矩阵乘法后加偏置

## 用法

```python
input = torch.randn(2, 4)
mat1 = torch.randn(2, 3)
mat2 = torch.randn(3, 4)

out = torch.addmm(input, mat1, mat2)

print(out.shape)
# torch.Size([2, 4])
```

计算逻辑：

```text
out = input + mat1 @ mat2
```

更完整形式：

```python
out = torch.addmm(input, mat1, mat2, beta=1, alpha=1)
```

等价于：

```text
out = beta * input + alpha * (mat1 @ mat2)
```

---

## 使用场景

### 1. 全连接层底层计算

```python
x = torch.randn(32, 128)
w = torch.randn(128, 64)
bias = torch.randn(64)

y = torch.addmm(bias, x, w)
print(y.shape)
# torch.Size([32, 64])
```

这里 `bias` 可以广播到 `(32, 64)`。

---

### 2. 性能优化

相比：

```python
y = x @ w + bias
```

`torch.addmm` 可以在一些后端上减少中间张量创建，有利于性能和内存。

---

# 14. `torch.baddbmm`：批量矩阵乘法后加输入

## 用法

```python
input = torch.randn(10, 2, 4)
batch1 = torch.randn(10, 2, 3)
batch2 = torch.randn(10, 3, 4)

out = torch.baddbmm(input, batch1, batch2)

print(out.shape)
# torch.Size([10, 2, 4])
```

计算逻辑：

```text
out = input + batch1 @ batch2
```

更完整形式：

```python
out = torch.baddbmm(input, batch1, batch2, beta=1, alpha=1)
```

等价于：

```text
out = beta * input + alpha * batch1.bmm(batch2)
```

---

## 使用场景

- batch attention 分数加 bias
- 批量线性变换后叠加残差
- 每个 batch 独立做矩阵乘加
- 性能敏感的批量矩阵运算

示例：

```python
bias = torch.randn(32, 20, 20)
Q = torch.randn(32, 20, 64)
K = torch.randn(32, 20, 64)

scores = torch.baddbmm(bias, Q, K.transpose(1, 2))
print(scores.shape)
# torch.Size([32, 20, 20])
```

---

# 15. `torch.addbmm`：多个 batch 矩阵乘法结果求和后加输入

## 用法

```python
input = torch.randn(2, 4)
batch1 = torch.randn(10, 2, 3)
batch2 = torch.randn(10, 3, 4)

out = torch.addbmm(input, batch1, batch2)

print(out.shape)
# torch.Size([2, 4])
```

计算逻辑：

```text
out = input + sum(batch1[i] @ batch2[i] for i in range(batch))
```

更完整形式：

```python
out = torch.addbmm(input, batch1, batch2, beta=1, alpha=1)
```

等价于：

```text
out = beta * input + alpha * sum(batch1_i @ batch2_i)
```

---

## 与 `baddbmm` 的区别

| 操作 | 输出形状 | 是否保留 batch 维 | 计算逻辑 |
|---|---:|---:|---|
| `torch.baddbmm` | `(b, m, p)` | 是 | `input + batch1 @ batch2` |
| `torch.addbmm` | `(m, p)` | 否 | `input + sum(batch1_i @ batch2_i)` |

---

## 使用场景

- 需要对 batch 维矩阵乘结果进行聚合
- 多个关系矩阵的累加
- 某些统计量计算
- 小批量矩阵乘结果汇总

---

# 16. `torch.multiply`：`torch.mul` 的别名

## 用法

```python
a = torch.tensor([1, 2, 3])
b = torch.tensor([4, 5, 6])

c = torch.multiply(a, b)
print(c)
# tensor([ 4, 10, 18])
```

`torch.multiply` 与 `torch.mul` 基本等价。

---

## 使用场景

主要是语义更直观：

```python
out = torch.multiply(x, mask)
```

适合希望代码读起来更像数学表达的场景。

---

# 17. `Tensor.mul_`：原地逐元素乘法

## 用法

```python
x = torch.tensor([1., 2., 3.])
x.mul_(2)

print(x)
# tensor([2., 4., 6.])
```

`mul_` 末尾带下划线，表示 **in-place 原地操作**，会直接修改原张量。

---

## 使用场景

- 减少内存开销
- 对临时张量做原地缩放
- 不需要保留原值时

---

## 注意事项

在涉及自动求导时，原地操作可能破坏计算图，导致报错：

```python
x = torch.randn(3, requires_grad=True)
y = x * 2
x.mul_(3)  # 可能导致 autograd 问题
```

在训练模型时，应谨慎使用 `mul_`。

---

# 18. 常见乘法操作对比表

| 操作 | 类型 | 输入维度要求 | 是否广播 | 输出 | 典型场景 |
|---|---|---|---|---|---|
| `a * b` | 逐元素乘法 | 形状相同或可广播 | 是 | 广播后的形状 | mask、特征加权 |
| `torch.mul` | 逐元素乘法 | 形状相同或可广播 | 是 | 广播后的形状 | 函数式逐元素乘 |
| `torch.multiply` | 逐元素乘法 | 形状相同或可广播 | 是 | 广播后的形状 | 同 `mul` |
| `torch.matmul` | 通用矩阵乘法 | 1D/2D/高维 | 是 | 根据矩阵乘规则 | 推荐优先使用 |
| `@` | 通用矩阵乘法 | 1D/2D/高维 | 是 | 同 `matmul` | 简洁矩阵乘 |
| `torch.mm` | 二维矩阵乘法 | 只能 2D | 否 | `(m, p)` | 明确二维矩阵乘 |
| `torch.bmm` | 批量矩阵乘法 | 只能 3D | 否 | `(b, m, p)` | batch 矩阵乘 |
| `torch.mv` | 矩阵乘向量 | 2D × 1D | 否 | `(m,)` | 单向量投影 |
| `torch.dot` | 向量点积 | 1D × 1D | 否 | 标量 | 相似度、打分 |
| `torch.outer` | 向量外积 | 1D × 1D | 否 | `(m, n)` | 特征交叉 |
| `torch.einsum` | 广义张量乘法 | 自定义 | 取决于表达式 | 自定义 | attention、高维 contraction |
| `torch.tensordot` | 指定维度收缩 | 高维张量 | 部分支持 | 取决于 dims | 科学计算、张量网络 |
| `torch.kron` | Kronecker 积 | 任意维 | 按规则扩展 | 维度乘积 | 结构化矩阵 |
| `torch.addmm` | 矩阵乘加 | 2D × 2D | input 可广播 | `(m, p)` | 全连接层、融合优化 |
| `torch.baddbmm` | 批量矩阵乘加 | 3D × 3D | input 可广播 | `(b, m, p)` | batch attention |
| `torch.addbmm` | batch 乘后求和再加 | 3D × 3D | input 可广播 | `(m, p)` | batch 结果聚合 |

---

# 19. 重点区别：`*`、`mm`、`bmm`、`matmul`

这是实际开发中最容易混淆的一组。

## 示例一：`*` 是逐元素乘法

```python
a = torch.randn(2, 3)
b = torch.randn(2, 3)

c = a * b
print(c.shape)
# torch.Size([2, 3])
```

计算的是：

```text
c[i, j] = a[i, j] * b[i, j]
```

---

## 示例二：`mm` 是二维矩阵乘法

```python
a = torch.randn(2, 3)
b = torch.randn(3, 4)

c = torch.mm(a, b)
print(c.shape)
# torch.Size([2, 4])
```

计算的是：

```text
c[i, j] = sum_k a[i, k] * b[k, j]
```

---

## 示例三：`bmm` 是三维 batch 矩阵乘法

```python
a = torch.randn(5, 2, 3)
b = torch.randn(5, 3, 4)

c = torch.bmm(a, b)
print(c.shape)
# torch.Size([5, 2, 4])
```

计算的是：

```text
c[b, i, j] = sum_k a[b, i, k] * b[b, k, j]
```

---

## 示例四：`matmul` 更通用

```python
a = torch.randn(5, 2, 3)
b = torch.randn(3, 4)

c = torch.matmul(a, b)
print(c.shape)
# torch.Size([5, 2, 4])
```

`matmul` 支持 batch 维广播，因此很多时候可以替代 `mm` 和 `bmm`。

---

# 20. 实际开发中如何选择

## 推荐选择规则

| 需求 | 推荐操作 |
|---|---|
| 逐元素相乘 | `*` 或 `torch.mul` |
| 加 mask | `x * mask` |
| 标量缩放 | `x * scalar` |
| 二维矩阵乘二维矩阵 | `torch.mm` 或 `@` |
| 希望接口通用 | `torch.matmul` 或 `@` |
| 三维 batch 矩阵乘法且无广播 | `torch.bmm` |
| batch 维需要广播 | `torch.matmul` |
| 矩阵乘向量 | `torch.mv` 或 `torch.matmul` |
| 向量点积 | `torch.dot` |
| 高维复杂维度乘法 | `torch.einsum` |
| 指定维度 contraction | `torch.tensordot` |
| 全连接层底层乘加 | `torch.addmm` |
| batch 矩阵乘加 | `torch.baddbmm` |

---

# 21. 常见坑点

## 1. 把 `*` 当成矩阵乘法

```python
a = torch.randn(2, 3)
b = torch.randn(3, 4)

c = a * b  # 报错或广播失败
```

矩阵乘法应使用：

```python
c = a @ b
# 或
c = torch.matmul(a, b)
# 或
c = torch.mm(a, b)
```

---

## 2. `bmm` 不支持广播

```python
a = torch.randn(8, 2, 3)
b = torch.randn(3, 4)

torch.bmm(a, b)  # 错误
```

应改为：

```python
torch.matmul(a, b)
```

---

## 3. `mm` 只支持二维

```python
a = torch.randn(8, 2, 3)
b = torch.randn(8, 3, 4)

torch.mm(a, b)  # 错误
```

应改为：

```python
torch.bmm(a, b)
# 或
torch.matmul(a, b)
```

---

## 4. Attention 中维度转置容易错

```python
Q = torch.randn(32, 20, 64)
K = torch.randn(32, 20, 64)

scores = torch.bmm(Q, K.transpose(1, 2))
print(scores.shape)
# torch.Size([32, 20, 20])
```

如果不转置：

```python
torch.bmm(Q, K)
```

会因为维度不匹配而报错。

---

## 5. `einsum` 表达式要保证下标语义一致

```python
Q = torch.randn(32, 8, 128, 64)
K = torch.randn(32, 8, 128, 64)

scores = torch.einsum('bhqd,bhkd->bhqk', Q, K)
```

这里 `d` 是要相乘求和的维度，不能写错成其他维度。

---

# 22. 简单决策流程

```text
是否逐元素相乘？
├─ 是：用 * / torch.mul / torch.multiply
└─ 否：
   是否是矩阵乘法？
   ├─ 是：
   │  ├─ 只有二维矩阵：torch.mm / @ / torch.matmul
   │  ├─ 三维 batch 矩阵：torch.bmm / torch.matmul
   │  ├─ 需要 batch 广播：torch.matmul
   │  └─ 只是向量点积：torch.dot
   └─ 否：
      是否是复杂高维张量收缩？
      ├─ 是：torch.einsum / torch.tensordot
      └─ 特殊线性代数：torch.outer / torch.kron
```

---

## 核心结论

- **`*` / `torch.mul` 是逐元素乘法，不是矩阵乘法。**
- **`torch.mm` 只适合二维矩阵乘法。**
- **`torch.bmm` 只适合三维 batch 矩阵乘法，不支持广播。**
- **`torch.matmul` / `@` 最通用，支持向量、矩阵、高维 batch 和广播。**
- **复杂高维乘法优先考虑 `torch.einsum`，表达能力最强。**
- **需要乘法后加 bias 或 input 时，可以考虑 `torch.addmm`、`torch.baddbmm` 等融合操作。**
